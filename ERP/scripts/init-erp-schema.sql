-- Schema dedicado del ERP en PostgreSQL (multi-schema Prisma).
-- Se ejecuta solo al inicializar el volumen del profile `db`.
-- En BD compartida existente, correr manualmente una vez:
--   CREATE SCHEMA IF NOT EXISTS erp;

CREATE SCHEMA IF NOT EXISTS erp;
